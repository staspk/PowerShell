py -m pip install --upgrade pip			=> 	Upgrade Pip
py -m venv .venv						=>  Create a Virtual Environment
.\.venv\Scripts\Activate.ps1			=> 	Activate Environment
py -m pip install -r requirements.txt	=>  Installs dependencies listed in requirements.txt
pip freeze > requirements.txt			=>  Create requirements.txt
pip install requests					=>  Example on how to add library to project
py -m ensurepip --upgrade				=>  How to find the "right" version of pip for PATH